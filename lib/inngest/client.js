import { Inngest } from "inngest";

// Create a client to send and receive events
//nothing to change dont touch it just run inngest for next day
export const inngest = new Inngest({ id: "preptic", name : "Preptic" });
